rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow any authenticated user to upload any file inside the chat_images directory.
    match /chat_images/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
